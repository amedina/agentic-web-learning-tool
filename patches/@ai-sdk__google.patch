diff --git a/dist/index.js b/dist/index.js
index 3bb868bcde9e81c3579854c6bed6fc1b14bf9647..d878448a13b2beea2a41c1cf090d08c6bc6bb1aa 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -716,6 +716,7 @@ function prepareTools({
     "gemini-pro-latest"
   ].some((id) => id === modelId);
   const isGemini2orNewer = modelId.includes("gemini-2") || modelId.includes("gemini-3") || modelId.includes("nano-banana") || isLatest;
+  const supportsToolCombination = modelId.includes('gemini-3');
   const supportsFileSearch = modelId.includes("gemini-2.5") || modelId.includes("gemini-3");
   if (tools == null) {
     return { tools: void 0, toolConfig: void 0, toolWarnings };
@@ -724,7 +725,13 @@ function prepareTools({
   const hasProviderDefinedTools = tools.some(
     (tool) => tool.type === "provider-defined"
   );
-  if (hasFunctionTools && hasProviderDefinedTools) {
+  const hasMixedTools = hasFunctionTools && hasProviderDefinedTools;
+  if (hasMixedTools && !supportsToolCombination) {
+    throw new import_provider3.UnsupportedFunctionalityError({
+        functionality: 'Gemini built-in tool and function combinations require a Gemini 3 model',
+      });
+  }
+  if (hasFunctionTools && hasProviderDefinedTools && !supportsToolCombination) {
     const functionTools = tools.filter((tool) => tool.type === "function");
     toolWarnings.push({
       type: "unsupported-tool",
@@ -732,7 +739,7 @@ function prepareTools({
       details: `Cannot mix function tools with provider-defined tools in the same request. Falling back to provider-defined tools only. The following function tools will be ignored: ${functionTools.map((t) => t.name).join(", ")}. Please use either function tools or provider-defined tools, but not both.`
     });
   }
-  if (hasProviderDefinedTools) {
+
     const googleTools2 = [];
     const providerDefinedTools = tools.filter(
       (tool) => tool.type === "provider-defined"
@@ -830,12 +837,6 @@ function prepareTools({
           break;
       }
     });
-    return {
-      tools: googleTools2.length > 0 ? googleTools2 : void 0,
-      toolConfig: void 0,
-      toolWarnings
-    };
-  }
   const functionDeclarations = [];
   for (const tool of tools) {
     switch (tool.type) {
@@ -858,30 +859,36 @@ function prepareTools({
       toolWarnings
     };
   }
+    const combinedTools = [
+    ...(functionDeclarations.length > 0 ? [{ functionDeclarations }] : []),
+    ...googleTools2,
+  ];
+
   const type = toolChoice.type;
   switch (type) {
     case "auto":
       return {
-        tools: [{ functionDeclarations }],
-        toolConfig: { functionCallingConfig: { mode: "AUTO" } },
+        tools: combinedTools.length > 0 ? combinedTools : undefined,
+        toolConfig: { includeServerSideToolInvocations: true, functionCallingConfig: { mode: "AUTO" } },
         toolWarnings
       };
     case "none":
       return {
-        tools: [{ functionDeclarations }],
-        toolConfig: { functionCallingConfig: { mode: "NONE" } },
+        tools: combinedTools.length > 0 ? combinedTools : undefined,
+        toolConfig: { includeServerSideToolInvocations: true, functionCallingConfig: { mode: "NONE" } },
         toolWarnings
       };
     case "required":
       return {
-        tools: [{ functionDeclarations }],
-        toolConfig: { functionCallingConfig: { mode: "ANY" } },
+        tools: combinedTools.length > 0 ? combinedTools : undefined,
+        toolConfig: { includeServerSideToolInvocations: true, functionCallingConfig: { mode: "ANY" } },
         toolWarnings
       };
     case "tool":
       return {
-        tools: [{ functionDeclarations }],
+        tools: combinedTools.length > 0 ? combinedTools : undefined,
         toolConfig: {
+          includeServerSideToolInvocations: true, 
           functionCallingConfig: {
             mode: "ANY",
             allowedFunctionNames: [toolChoice.toolName]
diff --git a/dist/index.mjs b/dist/index.mjs
index c8568ea55237be6a24967d584d85bd01f0b043d8..dbb2feb52aa598d1295e0cb809b97b0a8b2c7966 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -727,6 +727,7 @@ function prepareTools({
     "gemini-pro-latest"
   ].some((id) => id === modelId);
   const isGemini2orNewer = modelId.includes("gemini-2") || modelId.includes("gemini-3") || modelId.includes("nano-banana") || isLatest;
+  const supportsToolCombination = modelId.includes('gemini-3');
   const supportsFileSearch = modelId.includes("gemini-2.5") || modelId.includes("gemini-3");
   if (tools == null) {
     return { tools: void 0, toolConfig: void 0, toolWarnings };
@@ -735,7 +736,13 @@ function prepareTools({
   const hasProviderDefinedTools = tools.some(
     (tool) => tool.type === "provider-defined"
   );
-  if (hasFunctionTools && hasProviderDefinedTools) {
+  const hasMixedTools = hasFunctionTools && hasProviderDefinedTools;
+  if (hasMixedTools && !supportsToolCombination) {
+    throw new UnsupportedFunctionalityError2({
+        functionality: 'Gemini built-in tool and function combinations require a Gemini 3 model',
+      });
+  }
+  if (hasFunctionTools && hasProviderDefinedTools && !supportsToolCombination) {
     const functionTools = tools.filter((tool) => tool.type === "function");
     toolWarnings.push({
       type: "unsupported-tool",
@@ -743,7 +750,7 @@ function prepareTools({
       details: `Cannot mix function tools with provider-defined tools in the same request. Falling back to provider-defined tools only. The following function tools will be ignored: ${functionTools.map((t) => t.name).join(", ")}. Please use either function tools or provider-defined tools, but not both.`
     });
   }
-  if (hasProviderDefinedTools) {
+
     const googleTools2 = [];
     const providerDefinedTools = tools.filter(
       (tool) => tool.type === "provider-defined"
@@ -841,12 +848,6 @@ function prepareTools({
           break;
       }
     });
-    return {
-      tools: googleTools2.length > 0 ? googleTools2 : void 0,
-      toolConfig: void 0,
-      toolWarnings
-    };
-  }
   const functionDeclarations = [];
   for (const tool of tools) {
     switch (tool.type) {
@@ -869,30 +870,36 @@ function prepareTools({
       toolWarnings
     };
   }
+    const combinedTools = [
+    ...(functionDeclarations.length > 0 ? [{ functionDeclarations }] : []),
+    ...googleTools2,
+  ];
+
   const type = toolChoice.type;
   switch (type) {
     case "auto":
       return {
-        tools: [{ functionDeclarations }],
-        toolConfig: { functionCallingConfig: { mode: "AUTO" } },
+        tools: combinedTools.length > 0 ? combinedTools : undefined,
+        toolConfig: { includeServerSideToolInvocations: true, functionCallingConfig: { mode: "AUTO" } },
         toolWarnings
       };
     case "none":
       return {
-        tools: [{ functionDeclarations }],
-        toolConfig: { functionCallingConfig: { mode: "NONE" } },
+        tools: combinedTools.length > 0 ? combinedTools : undefined,
+        toolConfig: { includeServerSideToolInvocations: true, functionCallingConfig: { mode: "NONE" } },
         toolWarnings
       };
     case "required":
       return {
-        tools: [{ functionDeclarations }],
-        toolConfig: { functionCallingConfig: { mode: "ANY" } },
+        tools: combinedTools.length > 0 ? combinedTools : undefined,
+        toolConfig: { includeServerSideToolInvocations: true, functionCallingConfig: { mode: "ANY" } },
         toolWarnings
       };
     case "tool":
       return {
-        tools: [{ functionDeclarations }],
+        tools: combinedTools.length > 0 ? combinedTools : undefined,
         toolConfig: {
+          includeServerSideToolInvocations: true, 
           functionCallingConfig: {
             mode: "ANY",
             allowedFunctionNames: [toolChoice.toolName]
diff --git a/dist/internal/index.js b/dist/internal/index.js
index f3de90c9d652ef1876f9fff9626a0e76f7c9b4a7..796a4f837a6b6a3cd7b787046583e809b09db7e1 100644
--- a/dist/internal/index.js
+++ b/dist/internal/index.js
@@ -526,6 +526,7 @@ function prepareTools({
     "gemini-pro-latest"
   ].some((id) => id === modelId);
   const isGemini2orNewer = modelId.includes("gemini-2") || modelId.includes("gemini-3") || modelId.includes("nano-banana") || isLatest;
+  const supportsToolCombination = modelId.includes('gemini-3');
   const supportsFileSearch = modelId.includes("gemini-2.5") || modelId.includes("gemini-3");
   if (tools == null) {
     return { tools: void 0, toolConfig: void 0, toolWarnings };
@@ -534,7 +535,13 @@ function prepareTools({
   const hasProviderDefinedTools = tools.some(
     (tool) => tool.type === "provider-defined"
   );
-  if (hasFunctionTools && hasProviderDefinedTools) {
+  const hasMixedTools = hasFunctionTools && hasProviderDefinedTools;
+  if (hasMixedTools && !supportsToolCombination) {
+    throw new import_provider2.UnsupportedFunctionalityError({
+        functionality: 'Gemini built-in tool and function combinations require a Gemini 3 model',
+      });
+  }
+  if (hasFunctionTools && hasProviderDefinedTools && !supportsToolCombination) {
     const functionTools = tools.filter((tool) => tool.type === "function");
     toolWarnings.push({
       type: "unsupported-tool",
@@ -542,7 +549,7 @@ function prepareTools({
       details: `Cannot mix function tools with provider-defined tools in the same request. Falling back to provider-defined tools only. The following function tools will be ignored: ${functionTools.map((t) => t.name).join(", ")}. Please use either function tools or provider-defined tools, but not both.`
     });
   }
-  if (hasProviderDefinedTools) {
+
     const googleTools2 = [];
     const providerDefinedTools = tools.filter(
       (tool) => tool.type === "provider-defined"
@@ -640,12 +647,6 @@ function prepareTools({
           break;
       }
     });
-    return {
-      tools: googleTools2.length > 0 ? googleTools2 : void 0,
-      toolConfig: void 0,
-      toolWarnings
-    };
-  }
   const functionDeclarations = [];
   for (const tool of tools) {
     switch (tool.type) {
@@ -668,30 +669,36 @@ function prepareTools({
       toolWarnings
     };
   }
+    const combinedTools = [
+    ...(functionDeclarations.length > 0 ? [{ functionDeclarations }] : []),
+    ...googleTools2,
+  ];
+
   const type = toolChoice.type;
   switch (type) {
-    case "auto":
+case "auto":
       return {
-        tools: [{ functionDeclarations }],
-        toolConfig: { functionCallingConfig: { mode: "AUTO" } },
+        tools: combinedTools.length > 0 ? combinedTools : undefined,
+        toolConfig: { includeServerSideToolInvocations: true, functionCallingConfig: { mode: "AUTO" } },
         toolWarnings
       };
     case "none":
       return {
-        tools: [{ functionDeclarations }],
-        toolConfig: { functionCallingConfig: { mode: "NONE" } },
+        tools: combinedTools.length > 0 ? combinedTools : undefined,
+        toolConfig: { includeServerSideToolInvocations: true, functionCallingConfig: { mode: "NONE" } },
         toolWarnings
       };
     case "required":
       return {
-        tools: [{ functionDeclarations }],
-        toolConfig: { functionCallingConfig: { mode: "ANY" } },
+        tools: combinedTools.length > 0 ? combinedTools : undefined,
+        toolConfig: { includeServerSideToolInvocations: true, functionCallingConfig: { mode: "ANY" } },
         toolWarnings
       };
     case "tool":
       return {
-        tools: [{ functionDeclarations }],
+        tools: combinedTools.length > 0 ? combinedTools : undefined,
         toolConfig: {
+          includeServerSideToolInvocations: true, 
           functionCallingConfig: {
             mode: "ANY",
             allowedFunctionNames: [toolChoice.toolName]
diff --git a/dist/internal/index.mjs b/dist/internal/index.mjs
index 807801bea4c9634a8405dec58cdef0b04453222b..70346c5884607dca8d5f02d358b99b1cd2da050f 100644
--- a/dist/internal/index.mjs
+++ b/dist/internal/index.mjs
@@ -518,6 +518,7 @@ function prepareTools({
     "gemini-pro-latest"
   ].some((id) => id === modelId);
   const isGemini2orNewer = modelId.includes("gemini-2") || modelId.includes("gemini-3") || modelId.includes("nano-banana") || isLatest;
+  const supportsToolCombination = modelId.includes('gemini-3');
   const supportsFileSearch = modelId.includes("gemini-2.5") || modelId.includes("gemini-3");
   if (tools == null) {
     return { tools: void 0, toolConfig: void 0, toolWarnings };
@@ -526,7 +527,13 @@ function prepareTools({
   const hasProviderDefinedTools = tools.some(
     (tool) => tool.type === "provider-defined"
   );
-  if (hasFunctionTools && hasProviderDefinedTools) {
+  const hasMixedTools = hasFunctionTools && hasProviderDefinedTools;
+  if (hasMixedTools && !supportsToolCombination) {
+    throw new UnsupportedFunctionalityError2({
+        functionality: 'Gemini built-in tool and function combinations require a Gemini 3 model',
+      });
+  }
+  if (hasFunctionTools && hasProviderDefinedTools && !supportsToolCombination) {
     const functionTools = tools.filter((tool) => tool.type === "function");
     toolWarnings.push({
       type: "unsupported-tool",
@@ -534,7 +541,7 @@ function prepareTools({
       details: `Cannot mix function tools with provider-defined tools in the same request. Falling back to provider-defined tools only. The following function tools will be ignored: ${functionTools.map((t) => t.name).join(", ")}. Please use either function tools or provider-defined tools, but not both.`
     });
   }
-  if (hasProviderDefinedTools) {
+
     const googleTools2 = [];
     const providerDefinedTools = tools.filter(
       (tool) => tool.type === "provider-defined"
@@ -632,12 +639,6 @@ function prepareTools({
           break;
       }
     });
-    return {
-      tools: googleTools2.length > 0 ? googleTools2 : void 0,
-      toolConfig: void 0,
-      toolWarnings
-    };
-  }
   const functionDeclarations = [];
   for (const tool of tools) {
     switch (tool.type) {
@@ -660,30 +661,36 @@ function prepareTools({
       toolWarnings
     };
   }
+    const combinedTools = [
+    ...(functionDeclarations.length > 0 ? [{ functionDeclarations }] : []),
+    ...googleTools2,
+  ];
+
   const type = toolChoice.type;
   switch (type) {
-    case "auto":
+case "auto":
       return {
-        tools: [{ functionDeclarations }],
-        toolConfig: { functionCallingConfig: { mode: "AUTO" } },
+        tools: combinedTools.length > 0 ? combinedTools : undefined,
+        toolConfig: { includeServerSideToolInvocations: true, functionCallingConfig: { mode: "AUTO" } },
         toolWarnings
       };
     case "none":
       return {
-        tools: [{ functionDeclarations }],
-        toolConfig: { functionCallingConfig: { mode: "NONE" } },
+        tools: combinedTools.length > 0 ? combinedTools : undefined,
+        toolConfig: { includeServerSideToolInvocations: true, functionCallingConfig: { mode: "NONE" } },
         toolWarnings
       };
     case "required":
       return {
-        tools: [{ functionDeclarations }],
-        toolConfig: { functionCallingConfig: { mode: "ANY" } },
+        tools: combinedTools.length > 0 ? combinedTools : undefined,
+        toolConfig: { includeServerSideToolInvocations: true, functionCallingConfig: { mode: "ANY" } },
         toolWarnings
       };
     case "tool":
       return {
-        tools: [{ functionDeclarations }],
+        tools: combinedTools.length > 0 ? combinedTools : undefined,
         toolConfig: {
+          includeServerSideToolInvocations: true, 
           functionCallingConfig: {
             mode: "ANY",
             allowedFunctionNames: [toolChoice.toolName]
@@ -700,6 +707,7 @@ function prepareTools({
   }
 }
 
+
 // src/map-google-generative-ai-finish-reason.ts
 function mapGoogleGenerativeAIFinishReason({
   finishReason,
