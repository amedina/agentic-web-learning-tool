name: Tests

on:
  pull_request:
    branches:
      - main

# Cancels all prior workflow runs associated with pull requests that are still in progress.
concurrency:
  # The concurrency group contains the workflow name and the (target) branch name.
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    name: Test (${{ matrix.package }})
    # Change ubuntu version as the latest ubuntu is unable to run for some reason. Change it back to latest when its fixed.
    runs-on: 'ubuntu-24.04'
    permissions:
      contents: read # For actions/checkout to fetch code
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        # Only suites that currently pass are gated here. The following are
        # intentionally excluded until their test suites are fixed:
        #   - npm-advisor (test:npm-advisor): failing GitHub Issues URL assertion
        #   - engine-core (test:engine): TS errors + assertion failures
        #   - engine-extension (test:engine-extension): missing jest dep, suites fail to compile
        include:
          - package: extension
            script: test:extension
          - package: design-system
            script: test:design-system
    steps:
      - name: Harden Runner # Security agent: blocks outbound traffic & detects code overwrite to prevent breaches
        uses: step-security/harden-runner@17d0e2bd7d51742c71671bd19fa12bdc9d40a3d6
        with:
          disable-sudo: true
          disable-file-monitoring: true
          egress-policy: block
          allowed-endpoints: >
            api.github.com:443
            github.com:443
            registry.npmjs.org:443

      - name: Checkout
        uses: actions/checkout@c85c95e3d7251135ab7dc9ce3241c5835cc595a9

      - uses: pnpm/action-setup@v4
        name: Install pnpm
        with:
          version: 10.15.1
          run_install: false

      - name: Setup Node
        uses: actions/setup-node@64ed1c7eab4cce3362f8c340dee64e5eaeef8f7c
        with:
          node-version-file: '.nvmrc'
          cache: pnpm

      - name: Install dependencies
        run: pnpm i
        env:
          PUPPETEER_SKIP_DOWNLOAD: true

      - name: Run tests
        run: pnpm ${{ matrix.script }}
        env:
          NODE_OPTIONS: --max-old-space-size=4096
